[package]
name = "game_server"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = {version="1"}
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "signal", "io-util", "sync", "broadcast"] }
broadcast = {version = "0.1.0"}
aes-gcm = {version = "0.10"}